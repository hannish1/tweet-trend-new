pipeline {
    agent {
        node {
          label 'maven'
        }
    }

    stages {
        environment {

            PATH = "/opt/apache-maven-3.9.6/bin:$PATH"


        }
        stage('BUILD') {
            steps {
                sh 'mvn clean deploy'
            }
        }
    }
}
